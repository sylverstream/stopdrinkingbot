# Stopdrinking bot

# Local dev

## Prereqs
- NPM / NVM

## Setup
1. Run `npm install`
2. Create a .ENV file with this content:
```
BOT_USERNAME=...
BOT_PASSWORD=...
```
## Run locally

Run `npm start`.
